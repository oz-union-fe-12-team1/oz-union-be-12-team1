# git commit 전에 실행되는 훅
# black, isort 포맷터 적용
# mypy 정적 타입 검사

# Git Hook 에 연결하려면 아래 명령어 터미널에 입력
# pre-commit install

repos:
#   - repo: local
#     hooks:
#       - id: black
#         name: starting black
#         language: system
#         entry: uv run black
#         files: \.py$

#   - repo: local
#     hooks:
#       - id: isort
#         name: starting isort
#         language: system
#         entry: uv run isort
#         files: \.py$

  - repo: local
    hooks:
      - id: mypy
        name: starting mypy
        language: system
        entry: uv run python -m mypy --explicit-package-bases .
        files: \.py$
        pass_filenames: false
        always_run: true